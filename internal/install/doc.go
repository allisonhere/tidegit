// Package install holds the tests that exercise the repository's install.sh.
// The installer is a shell script rather than Go code, so it has no
// implementation here — only the harness that runs it against fake download
// tools.
package install
